{ "version":"0.0.0",
  "description":"Dispatch work to agents.",
  "name":"dynamo",
  "scripts": {
    "test": "crimp test/test.json -g",
    "generateDocs":"doc-tool package_header src_interface ./EXAMPLE.md > README.md"
  }
}
